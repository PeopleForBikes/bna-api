# bna-api

API for the BNA projects

## Components

This repository contains the following components:

- The PostgreSQL migrations
- The database schema documentation
- The generated Rust entities
- The lambda functions used for the API
- The Rust REST API client: bnaclient
